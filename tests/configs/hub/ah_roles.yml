---
ah_roles:
  - name: galaxy.stuff.mcstuffins
    description: test
    perms:
      - add_user
      - change_user
      - delete_user
      - view_user
      - add_ansiblerepository
      - add_collectionremote
  - name: galaxy.stuff.mcstuffins
    state: absent
...
