major_changes:
  - Adds Configuration as Code implementation role - A role to implement objects roles under a hierarchical and scalable directory structure.
  - Adds Configuration as Code desired state role - A role to manage the Controller's configuration desired state
  - Adds Configuration as Code from aap - A role to export and convert all  Controller's objects configuration in yaml files to be consumed with previous roles.
