---
- name: async_hub_ee_registry | An async_hub_ee_registry block
  block:
    - name: async_hub_ee_registry | Managing hub ee registry | Wait for finish the hub ee registry operation
      ansible.builtin.async_status:
        jid: "{{ __ee_registries_job_async_result_item.ansible_job_id }}"
      register: __ee_registries_job_async_result
      until: __ee_registries_job_async_result.finished
      retries: "{{ hub_configuration_ee_registry_async_retries }}"
      delay: "{{ hub_configuration_ee_registry_async_delay }}"
  rescue:
    - name: async_hub_ee_registry | Load error details
      ansible.builtin.include_vars:
        file: "{{ __ee_registries_job_async_result.results_file }}"
        name: __error_data

    - name: async_hub_ee_registry | Show error and stop execution
      when: not collect_logs
      ansible.builtin.fail:
        msg: "error: {{ __error_data['msg'] }}, response: {{ __error_data['response'] | default ('N/A') }}"

    - name: async_hub_ee_registry | Building list of not processed hub ee registry
      ansible.builtin.set_fact:
        __hub_ee_registry_error_list: "{{ __hub_ee_registry_error_list | default([]) + [
                                                                                  {
                                                                                   'name': __error_data['invocation']['module_args']['name'] | default(''),
                                                                                   'error': __error_data['msg'] | default('')
                                                                                  }
                                                                                 ]
                                      }}"
...
