---
collections:
  - name: awx.awx
    version: 13.0.0  # Correct??  we're not actually pulling the collection, its stored locally anyway
    # at the time of writing this comment, tower_application is an upstream PR anyway
