---
- name: async_gateway_service_node | An async_gateway_service_node block
  block:
    - name: async_gateway_service_node | Managing Service Node | Wait for finish the service node operation
      ansible.builtin.async_status:
        jid: "{{ __gateway_service_nodes_job_async_results_item.ansible_job_id }}"
      register: __gateway_service_nodes_job_async_result
      until: __gateway_service_nodes_job_async_result.finished
      retries: "{{ gateway_service_nodes_async_retries }}"
      delay: "{{ gateway_service_nodes_async_delay }}"
  rescue:
    - name: async_gateway_service_node | Load error details
      ansible.builtin.include_vars:
        file: "{{ __gateway_service_nodes_job_async_result.results_file }}"
        name: __error_data

    - name: async_gateway_service_node | Show error and stop execution
      when: not collect_logs
      ansible.builtin.fail:
        msg: "error: {{ __error_data['msg'] }}, response: {{ __error_data['response'] | default ('N/A') }}"

    - name: async_gateway_service_node | Building list of not processed service nodes
      ansible.builtin.set_fact:
        __gateway_service_node_error_list: "{{ __gateway_service_nodes_error_list | default([]) + [
                                                                                  {
                                                                                   'name': __error_data['invocation']['module_args']['name'] | default(''),
                                                                                   'error': __error_data['msg'] | default('')
                                                                                  }
                                                                                 ]
                                         }}"
...
