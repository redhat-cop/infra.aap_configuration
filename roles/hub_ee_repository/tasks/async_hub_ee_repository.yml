---
- name: async_hub_ee_repository | An async_hub_ee_repository block
  block:
    - name: async_hub_ee_repository | Managing hub ee repository | Wait for finish the hub ee repository operation
      ansible.builtin.async_status:
        jid: "{{ __ee_repositories_job_async_result_item.ansible_job_id }}"
      register: __ee_repositories_job_async_result
      until: __ee_repositories_job_async_result.finished
      retries: "{{ hub_configuration_ee_repository_async_retries }}"
      delay: "{{ hub_configuration_ee_repository_async_delay }}"
  rescue:
    - name: async_hub_ee_repository | Load error details
      ansible.builtin.include_vars:
        file: "{{ __ee_repositories_job_async_result.results_file }}"
        name: __error_data

    - name: async_hub_ee_repository | Show error and stop execution
      when: not collect_logs
      ansible.builtin.fail:
        msg: "error: {{ __error_data['msg'] }}, response: {{ __error_data['response'] | default ('N/A') }}"

    - name: async_hub_ee_repository | Building list of not processed hub ee repository
      ansible.builtin.set_fact:
        __hub_ee_repository_error_list: "{{ __hub_ee_repository_error_list | default([]) + [
                                                                                  {
                                                                                   'name': __error_data['invocation']['module_args']['name'] | default(''),
                                                                                   'error': __error_data['msg'] | default('')
                                                                                  }
                                                                                 ]
                                            }}"
...
