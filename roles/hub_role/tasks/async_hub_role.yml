---
- name: async_hub_role | An async_hub_role block
  block:
    - name: async_hub_role | Managing hub role | Wait for finish the hub role operation
      ansible.builtin.async_status:
        jid: "{{ __roles_job_async_result_item.ansible_job_id }}"
      register: __roles_job_async_result
      until: __roles_job_async_result.finished
      retries: "{{ hub_configuration_role_async_retries }}"
      delay: "{{ hub_configuration_role_async_delay }}"
  rescue:
    - name: async_hub_role | Load error details
      ansible.builtin.include_vars:
        file: "{{ __roles_job_async_result.results_file }}"
        name: __error_data

    - name: async_hub_role | Show error and stop execution
      when: not collect_logs
      ansible.builtin.fail:
        msg: "error: {{ __error_data['msg'] }}, response: {{ __error_data['response'] | default ('N/A') }}"

    - name: async_hub_role | Building list of not processed hub role
      ansible.builtin.set_fact:
        __hub_role_error_list: "{{ __hub_role_error_list | default([]) + [
                                                                            {
                                                                             'name': __error_data['invocation']['module_args']['name'] | default(''),
                                                                             'error': __error_data['msg'] | default('')
                                                                            }
                                                                           ]
                                }}"
...
