---
argument_specs:
  main:
    short_description: An Ansible Role to update, or destroy Automation Hub Collections.
    options:
      hub_collections:
        default: []
        required: false
        description: Data structure describing your collections to manage.
        type: list
        elements: dict
#        options:
#          namespace:
#            required: true
#            type: str
#            description: Namespace name. Must be lower case containing only alphanumeric characters and underscores.
#          name:
#            required: true
#            type: str
#            description: Collection name. Must be lower case containing only alphanumeric characters and underscores.
#          version:
#            required: false
#            type: str
#            description: Collection version. If not provided and auto_approve is true, will be derived from the path.
#          path:
#            required: false
#            type: str
#            description: Collection artifact file path.
#          repository:
#            required: false
#            type: str
#            default: staging
#            description: Name of the destination repository for the collection upload.
#          wait:
#            required: false
#            type: bool
#            default: true
#            description: Waits for the collection to be uploaded.
#          auto_approve:
#            required: false
#            type: bool
#            default: true
#            description: Approves a collection and requires version to be set.
#          timeout:
#            required: false
#            type: int
#            description: Maximum time in seconds to wait for the collection approval.
#          interval:
#            required: false
#            type: float
#            default: 10
#            description: Interval in seconds at which approval is checked.
#          overwrite_existing:
#            required: false
#            type: bool
#            default: false
#            description: Overwrites an existing collection and requires version to be set.
#          state:
#            required: false
#            type: str
#            default: present
#            choices:
#              - present
#              - absent
#            description: Desired state of the resource.

      # Async variables
      hub_configuration_collection_async_retries:
        default: "{{ aap_configuration_async_retries | default(50) }}"
        required: false
        description: This variable sets the number of retries to attempt for the role.
      aap_configuration_async_retries:
        default: 50
        required: false
        description: This variable sets number of retries across all roles as a default.
      hub_configuration_collection_async_delay:
        default: "{{ aap_configuration_async_delay | default(1) }}"
        required: false
        description: This variable sets delay between retries for the role.
      aap_configuration_async_delay:
        default: 1
        required: false
        description: This variable sets delay between retries across all roles as a default.
      aap_configuration_async_dir:
        default: null
        required: false
        description: Sets the directory to write the results file for async tasks. The default value is set to  `null` which uses the Ansible Default of `/root/.ansible_async/`.

      # No_log variables
      hub_configuration_collection_secure_logging:
        default: "{{ aap_configuration_secure_logging | default(false) }}"
        required: false
        type: bool
        description: Whether or not to include the sensitive role tasks in the log. Set this value to `true` if you will be providing your sensitive values from elsewhere.
      aap_configuration_secure_logging:
        default: false
        required: false
        type: bool
        description: This variable enables secure logging across all roles as a default.

      # Generic across all roles
      aap_hostname:
        default: None
        required: false
        description: URL to the Ansible Automation Platform Server.
        type: str
      hub_path_prefix:
        required: false
        description: The path for the Automation Hub API. Usually galaxy or automation-hub unless custom set in AH settings.
      aap_validate_certs:
        default: true
        required: false
        description: Whether or not to validate the Ansible Automation Platform Server's SSL certificate.
        type: str
      aap_request_timeout:
        default: 10
        required: false
        description: Specify the timeout Ansible should use in requests to the Ansible Automation Platform Server.
        type: float
      aap_username:
        default: None
        required: false
        description: Admin User on the Ansible Automation Platform Server. Either username / password or oauthtoken need to be specified.
        type: str
      aap_password:
        default: None
        required: false
        description: Controller Admin User's password on the Ansible Automation Platform Server. This should be stored in an Ansible Vault at vars/controller-secrets.yml or elsewhere and called from a parent playbook. Either username / password or oauthtoken need to be specified.
        type: str
      hub_token:
        default: None
        required: false
        description: Controller Admin User's token on the Ansible Automation Platform Server. This should be stored in an Ansible Vault at or elsewhere and called from a parent playbook. Either username / password or oauthtoken need to be specified.
        type: str
...
