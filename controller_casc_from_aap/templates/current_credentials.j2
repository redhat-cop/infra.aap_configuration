---
controller_credentials:
{% for credential in current_asset_value %}
  - name: "{{ credential.name }}"
    description: "{{ credential.description }}"
    credential_type: "{{ credential.credential_type.name }}"
{% if credential.organization is defined and credential.organization is not none %}
    organization: "{{ credential.organization.name }}"
{% endif %}
    inputs:
{{ credential.inputs | to_nice_yaml(indent=2) | indent(width=6, first=True) }}
{% endfor %}
...
