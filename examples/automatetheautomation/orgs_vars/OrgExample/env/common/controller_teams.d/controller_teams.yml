---
controller_teams:
  - name: "{{ orgs }} {{ orgs }}-team"
    description: "Team for {{ orgs }} Org"
    organization: "{{ orgs }}"

  - name: "{{ orgs }} test-group"
    description: "test-group"
    organization: "{{ orgs }}"
...
